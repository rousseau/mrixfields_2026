# ─────────────────────────────────────────────────────────────────────────────
# Environnement DGX Station (debug multi-GPU local)
#
# Utilisation single-GPU :
#   python src/cfm/train_cfm_3d.py --config configs/cfm3d_T1W_aekl.yaml --env dgx
#
# Multi-GPU (torchrun) :
#   torchrun --nproc_per_node=4 src/cfm/train_cfm_3d.py \
#       --config configs/cfm3d_T1W_aekl.yaml --env dgx
#
# Ou via le script de lancement :
#   bash src/slurm/launch_cfm3d_dgx.sh vae T1W 2    # 2 GPUs, phase VAE
#   bash src/slurm/launch_cfm3d_dgx.sh cfm T1W 4    # 4 GPUs, phase CFM
# ─────────────────────────────────────────────────────────────────────────────

# Données brutes MRIxFields 2026 (NIfTI)
data_root: /home/rousseau/Data/MRIxFields_20260414

# Racine des sorties (mêmes que local — les expériences DGX s'intègrent)
output_root: /home/rousseau/Exp/mrixfields_2026/outputs

# Racine du projet
project_root: /home/rousseau/Exp/mrixfields_2026

# Exécutable Python
python: /home/rousseau/miniforge3/bin/python
